# Результат сборки — целиком генерируется из src/ командой npm run build.
# В гит не идёт: два файла по 12 МБ, переписываемые при каждой сборке,
# раздули бы историю за неделю.
dist/
send/

# Запасные кадры: лежат рядом с рабочими, в сборку не идут
alt/

node_modules/

.DS_Store
Thumbs.db
